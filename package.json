{
  "name": "dsh-browser-firefox-workspace",
  "version": "0.3.3",
  "description": "Firefox edition of the dsh browser bridge: plugin + WebExtension workspace",
  "author": "tuojc",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "build": "pnpm --filter dsh-browser-firefox run build && pnpm --filter dsh-browser-extension run build",
    "start": "dsh web",
    "test": "pnpm --filter dsh-browser-firefox run test && pnpm --filter dsh-browser-extension run test",
    "typecheck": "pnpm --filter dsh-browser-firefox run typecheck && pnpm --filter dsh-browser-extension run typecheck"
  },
  "dependencies": {
    "@deepseek-ai/dsh": "0.1.1-rc.1"
  }
}
